
<% if @users_comments.blank? %>
	$("#viewCommentModalBody").replaceWith("Comments not available.");
<% else %>
	$("#viewCommentModalBody").replaceWith("<%= escape_javascript(render @users_comments) %>");
<% end %>
